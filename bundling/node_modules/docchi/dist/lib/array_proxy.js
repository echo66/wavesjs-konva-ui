'use strict';

var _Array$find = require('babel-runtime/core-js/array/find')['default'];

Object.defineProperty(exports, '__esModule', {
  value: true
});
exports.find = find;
var hasFind = ('find' in Array.prototype);

/**
 * Proxy for Array.prototype.find.
 *
 * @param {Array} array
 * @param {Function} fn
 */

function find(array, fn) {
  // Check for ES6 `Array.prototype.find`, or fall back to `core-js` method.
  return hasFind ? array.find(fn) : _Array$find(array, fn);
}